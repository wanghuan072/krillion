# Flow 06 handoff — development

Status: **complete with expected release-gate failure**

The functional Next.js implementation, strict draft data, shared player, data-driven groups, responsive shell, Guide pipeline, Legal routes, CSP derivation, validators, unit tests, and browser tests are in place. Development validation, typecheck, lint, unit tests, and draft browser tests pass. `npm run build` correctly stops before Next compilation because Flow 07–09 release content is not yet complete.

Important correction: provider discovery URLs were launch pages that opened the game in a new tab. Each additional Game now uses the actual current-version Famobi CDN frame URL reached by that link, so its player can remain inside the site. Flow 10 must still complete one effective gameplay input per addition.

Logo and social art are real local files produced with the imagegen skill and match the depth-line design signature. Flow 08 must register them and all game/Guide media in the common ledger.

Proceed to Flow 07 without changing navigation, routes, visual tokens, player addresses, or the eight-game set unless second-pass research exposes a true content/risk/video blocker.
